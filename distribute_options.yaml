output: dist/
releases:
  - name: dev
    jobs:
      - name: dupot-easy-flatpak-deb
        package:
          platform: linux
          target: deb
          build_args:
            enable-experiment: records
